services:
  backend:
    command: python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./:/app:rw
    environment:
      - ENVIRONMENT=development
      - PYTHONUNBUFFERED=1


